@*@model List<MyInvoiceWebApp.Models.Book>*@

@{
    ViewData["Title"] = "Home Page";
}

<br />
<div class="container row p-0 m-0">
    <div class="col-6">
        <h2 class="text-info">Book List</h2>
    </div>
    <div class="col-3 offset-3">
        <a asp-action="Insert" asp-controller="Invoice" class="btn btn-info form-control text-white">
            Add New Bill
        </a>
    </div>
    <div class="col-12 border p-3">
        <table id="DT_load" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Author</th>
                    <th>ISBN</th>
                    <th></th>
                </tr>
            </thead>
            @*<tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@Html.DisplayFor(modelitem => item.Name)</td>
                        <td>@Html.DisplayFor(modelitem => item.Author)</td>
                        <td>@Html.DisplayFor(modelitem => item.ISBN)</td>
                        <td>
                            @Html.ActionLink("Edit", "Edit", new { }) | @Html.ActionLink("Edit", "Details", new { }) | @Html.ActionLink("Edit", "Delete", new { })
                        </td>
                    </tr>
                }
            </tbody>*@
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/bookList.js"></script>
}